const Usuario = require('../models/Usuario');
const jwt = require('jsonwebtoken');
require('dotenv').config();

const registro = async (req, res) => { // registreo
};

const login = async (req, res) => { // logineo
};

module.exports = { registro, login };
